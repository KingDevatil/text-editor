# Text Editor 使用说明

## 命令面板

| 快捷键 | 功能 |
|--------|------|
| `F1` | 打开命令面板（快速访问所有功能） |

---

## 文本编辑

| 快捷键 | 功能 |
|--------|------|
| `Ctrl + Z` | 撤销 |
| `Ctrl + Shift + Z` | 重做 |
| `Ctrl + X` | 剪切 |
| `Ctrl + C` | 复制 |
| `Ctrl + V` | 粘贴 |
| `Ctrl + A` | 全选 |
| `Tab` / `Shift + Tab` | 缩进 / 取消缩进 |
| `Shift + Alt + F` | 格式化文档 |

---

## 查找与替换

| 快捷键 | 功能 |
|--------|------|
| `Ctrl + F` | 查找 |
| `Ctrl + H` | 替换 |

**高级功能：**

- **正则表达式搜索**：在查找替换面板中切换正则模式后即可使用
- **正则构建器**：可视化辅助构建正则表达式
- **选中文本自动填入**：选中文本后按 `Ctrl + F` 可自动将选中内容填入查找框

---

## 多光标与列编辑（Notepad++ 风格）

| 快捷键 | 功能 |
|--------|------|
| `Ctrl + 点击` | 添加多个光标（多光标编辑） |
| `Alt + 拖拽` | 矩形框选（列编辑） |
| `Ctrl + D` | 选中当前单词，继续按选中下一个匹配 |

---

## 代码导航与辅助

| 快捷键 | 功能 |
|--------|------|
| `F12` | 转到定义（仅桌面端） |

**辅助功能：**

- **自动补全**：输入时自动提示代码补全（`Ctrl + Space` 手动触发）
- **签名提示**：输入函数参数时显示参数列表和说明
- **悬停提示**：鼠标悬停在变量/函数上查看类型和信息
- **括号匹配**：自动高亮匹配括号与 HTML/XML 标签对
- **代码折叠**：点击行号旁的箭头折叠/展开代码块

---

## 文件与标签

| 快捷键 | 功能 |
|--------|------|
| `Ctrl + N` | 新建文件 |
| `Ctrl + O` | 打开文件 |
| `Ctrl + S` | 保存 |
| `Ctrl + W` | 关闭当前标签 |
| `鼠标拖拽标签` | 同组内排序 / 跨组移动（分屏） |

**文件相关功能：**

- **Diff 对比**：选择两个文件进行差异对比（命令面板或工具栏）
- **外部修改检测**：文件被外部程序修改时自动提示重新加载
- **在文件夹中显示**：通过命令面板在系统文件管理器中定位当前文件

---

## 视图与分屏

| 快捷键 | 功能 |
|--------|------|
| `Ctrl + Shift + V` | 切换 Markdown / HTML 阅读模式 |
| `F11` | 切换全屏 |
| `鼠标拖拽标签到另一侧` | 分屏编辑 |

**视图功能：**

- **预览模式**：Markdown / HTML 实时预览（工具栏或命令面板）
- **Minimap**：代码缩略图，支持快速定位（设置面板开关）
- **侧边栏**：显示/隐藏项目文件树（工具栏或命令面板）

---

## Markdown / HTML

### 格式快捷键（需要切换语言为 Markdown）

| 快捷键 | 功能 |
|--------|------|
| `Ctrl + 1 ~ 6` | 标题 1~6 |
| `Ctrl + B` | 粗体 |
| `Ctrl + I` | 斜体 |
| `Ctrl + U` | 删除线 |
| `Ctrl + K` | 链接 |
| <code>Ctrl + \`</code> | 行内代码 |

### 预览与阅读模式

支持 Markdown 和 HTML 的实时预览与阅读模式：

- **预览模式**：与编辑器并排显示渲染后的内容
- **阅读模式**：专注阅读，带目录导航、字体调节和页宽切换（默认 / 较宽 / 全宽）
- **文档内目录锚点链接**（`[标题](#标题)`）可点击跳转
- **主题自适应**：阅读模式下支持深色/浅色主题自适应

---

*更多功能持续开发中，如有建议欢迎反馈。*
